first we have to install the below code
pip install opencv-python face-recognition numpy tqdm
I recommend that u can use python version on 9 which can only support dlib python library and also cmake

